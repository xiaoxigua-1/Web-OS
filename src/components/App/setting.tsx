import React from "react";
import { App } from "../../type/app";

export default class Setting extends App {
    render() {
        return(
            <div id="setting">
            </div>
        );
    }
}